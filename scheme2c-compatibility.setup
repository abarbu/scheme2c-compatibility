;;;  -*- Hen -*-

(use setup-helper-mod)

(verify-extension-name "scheme2c-compatibility")

(setup-shared-extension-module 'scheme2c-compatibility (extension-version "1.0")
  #:inline? #t #:types? #t
  #:compile-options '(-O3 -d2))
